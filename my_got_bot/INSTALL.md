# Инструкция по запуску

## Быстрый старт (автоматический)

Используйте скрипт автоматической установки:

```bash
cd my_got_bot
./start.sh
```

Скрипт автоматически:
1. Проверит наличие Python
2. Установит зависимости
3. Создаст .env файл из .env.example (если его нет)
4. Запустит бота

## Ручная установка

### 1. Установите зависимости

```bash
cd my_got_bot
pip install requests python-dotenv
```

Или используйте requirements.txt:

```bash
pip install -r requirements.txt
```

### 2. Создайте .env файл

Скопируйте пример:

```bash
cp .env.example .env
```

### 3. Добавьте API ключ

Откройте `.env` и замените `your_api_key_here` на ваш настоящий ключ:

```
TOGETHER_API_KEY=ваш_реальный_ключ_здесь
```

**Где взять ключ:**
👉 https://api.together.xyz/settings/api-keys

### 4. Запустите бота

```bash
python main.py
```

## Использование

После запуска просто вводите ваши вопросы в консоли.

**Команды:**
- `exit`, `quit`, `q`, `выход` — выход из чата

**Пример работы:**

```
============================================================
🤖 Chain-of-Thought Chatbot (Together.ai)
============================================================
Введите 'exit' или 'quit' для выхода

============================================================
=== INBOX ===
============================================================
Вы: Что такое квантовая запутанность?

🔄 Итерация 1/4

============================================================
=== ENGINE ===
============================================================
🤖 Отправка запроса к meta-llama/Meta-Llama-3.1-70B-Instruct-Turbo...
📝 Используется промпт: cot_initial.txt
✅ Ответ получен

============================================================
=== MOUTH ===
============================================================
💭 Ещё думаю...
...

🔄 Итерация 2/4
...

============================================================
=== MOUTH ===
============================================================
🗣️  ФИНАЛЬНЫЙ ОТВЕТ:

Квантовая запутанность — это явление, при котором...

💾 Сохранено в память (1 обменов)
```

## Настройка

### Изменение модели

Отредактируйте `config.py`:

```python
MODEL_NAME = "meta-llama/Meta-Llama-3.1-8B-Instruct-Turbo"  # Меньшая модель
```

### Изменение количества итераций

Отредактируйте `config.py`:

```python
MAX_COT_ITERATIONS = 6  # Больше шагов размышлений
```

### Изменение размера памяти

Отредактируйте `main.py`:

```python
chat_memory = ChatMemory(max_exchanges=50)  # Больше истории
```

### Редактирование промптов

Промпты хранятся в текстовых файлах для удобного редактирования:

- `engine/prompts/cot_initial.txt` — первая итерация
- `engine/prompts/cot_refine.txt` — итерации 2-4

## Устранение проблем

### Ошибка: "TOGETHER_API_KEY не найден"

Убедитесь, что:
1. Файл `.env` существует
2. В нём есть строка `TOGETHER_API_KEY=ваш_ключ`
3. Ключ валидный (получен на together.ai)

### Ошибка: "Could not reach Together.ai API"

Проверьте:
1. Интернет соединение
2. Валидность API ключа
3. Статус сервиса: https://api.together.xyz/

### Ошибка импорта модулей

Переустановите зависимости:

```bash
pip install --upgrade requests python-dotenv
```

## Архитектура

```
Пользователь → inbox → eyes → memory → [brain + engine (CoT loop)] → mouth → Пользователь
                                ↓
                          chat_memory сохраняет обмен
```

Каждый запрос проходит до 4 итераций размышлений до получения `FINAL_ANSWER:`.

## Доступные модели Together.ai

Рекомендуемые модели:
- `meta-llama/Meta-Llama-3.1-70B-Instruct-Turbo` (по умолчанию)
- `meta-llama/Meta-Llama-3.1-8B-Instruct-Turbo` (быстрее, дешевле)
- `meta-llama/Llama-3-70b-chat-hf`

Полный список: https://docs.together.ai/docs/inference-models
