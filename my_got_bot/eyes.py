"""
ЧТО ДЕЛАЕТ ЭТОТ ФАЙЛ / МОДУЛЬ:
Заглушка для будущей обработки визуального контента (картинок, скриншотов).
На данный момент просто возвращает текстовое сообщение без изменений, но в будущем
здесь будет логика для распознавания изображений через vision-модели и извлечения
текстового описания визуального контента для добавления в контекст разговора.
"""


def process_visual_content(message):
    """
    Обрабатывает визуальный контент в сообщении.
    Пока что это заглушка — просто возвращает текст как есть.
    
    В будущем здесь будет:
    - Определение наличия изображений/ссылок на картинки
    - Отправка их в vision-модель
    - Получение текстового описания
    - Добавление описания в контекст
    """
    print("\n" + "=" * 60)
    print("=== EYES (заглушка) ===")
    print("=" * 60)
    print("👁️  Визуальная обработка пока не активна")
    
    # Просто возвращаем текст без изменений
    return message
