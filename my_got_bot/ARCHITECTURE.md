# Описание компонентов системы

## 🎯 Общий поток данных

```
User Input → INBOX → EYES → MEMORY → BRAIN ⟷ ENGINE (CoT) → MOUTH → User Output
                                       ↑                            ↓
                                       └──────── ChatMemory ────────┘
```

---

## 📦 Модули системы

### 1. **main.py** — Главный оркестратор
**Назначение:** Точка входа, бесконечный цикл общения

**Что делает:**
- Запускает бесконечный цикл взаимодействия
- Координирует работу всех компонентов
- Управляет итерациями CoT (до 4 раз)
- Обрабатывает исключения и выход

**Зависимости:** все остальные модули

---

### 2. **config.py** — Конфигурация
**Назначение:** Центральное место для настроек

**Содержит:**
- `TOGETHER_API_KEY` — API ключ
- `MODEL_NAME` — название модели LLM
- `MAX_COT_ITERATIONS` — лимит итераций

**Откуда берёт:** `.env` файл (через python-dotenv)

---

### 3. **inbox.py** — Приёмник сообщений
**Назначение:** Получение и валидация пользовательского ввода

**Функция:** `get_user_message()`
- Читает из `input()`
- Проверяет на команды выхода
- Валидирует непустое сообщение
- Возвращает `str` или `None` (выход)

---

### 4. **eyes.py** — Обработка визуального контента
**Назначение:** Будущая интеграция vision-моделей

**Текущее состояние:** Заглушка
**Функция:** `process_visual_content(message)`
- Сейчас: просто возвращает текст
- Будущее: распознавание картинок, OCR, vision API

---

### 5. **memory/** — Система памяти

#### 5.1. **memory/chat_memory.py** — Краткосрочная память
**Назначение:** Хранит последние N обменов

**Класс:** `ChatMemory`
- `__init__(max_exchanges=20)` — инициализация
- `add_exchange(user, bot)` — добавить обмен
- `get_formatted_history()` — получить историю для промпта
- `clear()` — очистить
- `__len__()` — количество обменов

**Структура:** `[(user_msg, bot_response), ...]`

#### 5.2. **memory/big_memory.py** — Долгосрочная память
**Назначение:** Хранение между сеансами

**Текущее состояние:** Заглушка
**Будущее:**
- Векторная БД
- RAG (Retrieval-Augmented Generation)
- Персистентное хранилище фактов

---

### 6. **brain.py** — Рабочая память рассуждений

#### 6.1. **BrainText** — Текстовая цепочка мыслей
**Назначение:** Хранит текущий Chain-of-Thought

**Методы:**
- `add_step(thought)` — добавить шаг размышлений
- `get_chain()` — получить всю цепочку
- `clear()` — очистить для нового запроса
- `display()` — красиво вывести в консоль

**Структура:**
- `self.chain` — полный текст CoT
- `self.steps` — массив шагов (для отладки)

#### 6.2. **BrainGraph** — Графовая структура
**Текущее состояние:** Заглушка
**Будущее:** Древовидные рассуждения, возврат к узлам

---

### 7. **engine/** — Движок размышлений

#### 7.1. **engine/engine.py** — Главный движок
**Назначение:** Один шаг CoT размышлений

**Функция:** `think_one_step(user_message, history, current_cot, is_first_step)`

**Алгоритм:**
1. Загружает промпт из файла (`cot_initial.txt` или `cot_refine.txt`)
2. Подставляет переменные: `{history}`, `{user_message}`, `{current_cot}`
3. Формирует API запрос к Together.ai
4. Отправляет POST на `https://api.together.xyz/v1/chat/completions`
5. Возвращает ответ модели

**Вспомогательная функция:** `load_prompt(filename)`
- Читает промпт из `engine/prompts/`
- Обрабатывает ошибки файлов

#### 7.2. **engine/prompts/** — Шаблоны промптов

**cot_initial.txt** — Промпт для первой итерации
**cot_refine.txt** — Промпт для итераций 2-4

**Структура промпта:**
```
You are a very intelligent, empathetic and slightly sarcastic assistant. 
Always use Chain-of-Thought.

Conversation history: {history}
New user message: {user_message}
Current chain of thought: {current_cot}

Do exactly ONE improvement step.
If you are 100% sure — end with a separate line:
FINAL_ANSWER: <your final reply>

Otherwise just continue the chain.
```

**Переменные подстановки:**
- `{history}` — история последних 20 обменов
- `{user_message}` — текущий вопрос
- `{current_cot}` — накопленная цепочка мыслей

---

### 8. **mouth.py** — Вывод финального ответа
**Назначение:** Извлечение и форматирование ответа

**Функции:**

#### `extract_final_answer(response_text)`
- Ищет маркер `FINAL_ANSWER:` в тексте
- Возвращает `(answer, is_final)`
- `is_final=True` → нашли финальный ответ
- `is_final=False` → промежуточные мысли

#### `speak(response_text)`
- Красиво выводит ответ в консоль
- С маркером → "🗣️ ФИНАЛЬНЫЙ ОТВЕТ"
- Без маркера → "💭 Ещё думаю..."

---

## 🔄 Жизненный цикл запроса

### Шаг 1: Приём
```python
user_message = get_user_message()  # inbox.py
```

### Шаг 2: Обработка визуала
```python
processed = process_visual_content(user_message)  # eyes.py
```

### Шаг 3: Загрузка контекста
```python
history = chat_memory.get_formatted_history()  # memory/chat_memory.py
```

### Шаг 4: Итерации CoT (до 4 раз)
```python
for i in range(1, MAX_COT_ITERATIONS + 1):
    response = think_one_step(
        user_message=processed,
        history=history,
        current_cot=brain.get_chain(),
        is_first_step=(i == 1)
    )  # engine/engine.py
    
    brain.add_step(response)  # brain.py
    answer, is_final = speak(response)  # mouth.py
    
    if is_final:
        break
```

### Шаг 5: Сохранение в память
```python
chat_memory.add_exchange(user_message, final_answer)
```

---

## 🎨 Форматирование вывода

Все модули используют визуальные разделители:

```
============================================================
=== INBOX ===
============================================================
```

**Модули:**
- `INBOX` — приём сообщения
- `EYES` — обработка визуала
- `BRAIN` — цепочка мыслей
- `ENGINE` — работа движка
- `MOUTH` — финальный ответ

**Эмодзи индикаторы:**
- 🤖 — отправка к AI
- ✅ — успешно
- ❌ — ошибка
- 💭 — думаю
- 🗣️ — финальный ответ
- 💾 — сохранено
- 🔄 — итерация

---

## 🔧 Технические детали

### API Together.ai

**Endpoint:** `https://api.together.xyz/v1/chat/completions`

**Формат запроса:**
```json
{
  "model": "meta-llama/Meta-Llama-3.1-70B-Instruct-Turbo",
  "messages": [
    {"role": "user", "content": "промпт с контекстом"}
  ],
  "temperature": 0.7,
  "max_tokens": 1024
}
```

**Формат ответа:**
```json
{
  "choices": [
    {
      "message": {
        "content": "ответ модели"
      }
    }
  ]
}
```

### Зависимости

**requests** — HTTP клиент для API запросов
**python-dotenv** — загрузка переменных из .env

---

## 📈 Расширяемость

### Добавление новых компонентов:

1. **Голосовой ввод/вывод** → модули `ears.py` / `voice.py`
2. **Vision API** → реализовать `eyes.py`
3. **Долгосрочная память** → реализовать `big_memory.py`
4. **Web-интерфейс** → FastAPI вместо терминала
5. **Графовые рассуждения** → реализовать `BrainGraph`

### Добавление новых типов промптов:

Создайте файл в `engine/prompts/`:
- `cot_creative.txt` — для творческих задач
- `cot_analytical.txt` — для анализа
- `cot_coding.txt` — для программирования

Модифицируйте `engine.py` для выбора промпта по типу задачи.

---

## 🐛 Точки отладки

### Если ответы странные:
1. Проверьте `engine/prompts/` — корректность промптов
2. Добавьте `brain.display()` после каждой итерации
3. Выведите `history` перед отправкой в engine

### Если не находит FINAL_ANSWER:
1. Увеличьте `MAX_COT_ITERATIONS`
2. Измените промпт — сделайте инструкцию яснее
3. Уменьшите `temperature` в `engine.py` (для более детерминированных ответов)

### Если съедает много токенов:
1. Уменьшите `max_exchanges` в ChatMemory
2. Уменьшите `max_tokens` в API запросе
3. Обрезайте старые части `current_cot`

---

## 📚 Дальнейшее развитие

### Фаза 2: Vision
- Интеграция LLaVA или GPT-4 Vision
- Обработка скриншотов и картинок
- OCR для текста на изображениях

### Фаза 3: Долгосрочная память
- Векторная БД (ChromaDB / Pinecone)
- RAG для поиска по прошлым разговорам
- Персональные факты о пользователе

### Фаза 4: Графовые рассуждения
- Tree-of-Thoughts вместо линейного CoT
- Параллельные ветки размышлений
- Возврат к предыдущим узлам

### Фаза 5: Мультиагентность
- Несколько специализированных агентов
- Координация через main.py
- Передача задач между агентами
