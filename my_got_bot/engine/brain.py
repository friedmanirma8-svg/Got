"""
ЧТО ДЕЛАЕТ ЭТОТ ФАЙЛ / МОДУЛЬ:
Хранит текущую цепочку мыслей (Chain of Thought) для обрабатываемого запроса.
Это как рабочая память бота — здесь накапливаются промежуточные рассуждения,
которые модель генерирует при обработке сложного вопроса. На каждой итерации
цепочка дополняется новыми мыслями, пока модель не придёт к финальному ответу.
"""


class BrainText:
    """
    Хранит текстовую цепочку мыслей (Chain of Thought).
    На каждом шаге размышлений добавляется новый текстовый фрагмент.
    """
    
    def __init__(self):
        """
        Инициализирует пустую цепочку мыслей.
        """
        self.chain = ""  # Текущая цепочка рассуждений
        self.steps = []  # История шагов (для отладки)
    
    def add_step(self, thought):
        """
        Добавляет новый шаг размышлений в цепочку.
        """
        if self.chain:
            self.chain += "\n\n"
        self.chain += thought
        self.steps.append(thought)
    
    def get_chain(self):
        """
        Возвращает текущую цепочку мыслей.
        """
        return self.chain
    
    def clear(self):
        """
        Очищает цепочку для нового запроса.
        """
        self.chain = ""
        self.steps = []
    
    def display(self):
        """
        Красиво выводит текущую цепочку мыслей в консоль.
        """
        print("\n" + "=" * 60)
        print("=== BRAIN ===")
        print("=" * 60)
        if self.chain:
            print(f"🧠 Chain of Thought ({len(self.steps)} шагов):")
            print(self.chain)
        else:
            print("🧠 Цепочка мыслей пока пуста")
        print("=" * 60)


# В будущем здесь будет BrainGraph для представления мыслей в виде графа
class BrainGraph:
    """
    Заглушка для будущей графовой структуры цепочки мыслей.
    Позволит создавать ветвящиеся рассуждения и возвращаться к предыдущим узлам.
    """
    
    def __init__(self):
        print("🌳 BrainGraph пока не реализован")
